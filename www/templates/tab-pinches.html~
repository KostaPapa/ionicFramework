<ion-view view-title="Pinches">
  <ion-content>
    <ion-list>
      <ion-item class="item-remove-animate item-avatar item-icon-right" ng-repeat="pinch in pinches" type="item-text-wrap" href="#/tab/pinches/{{pinch.id}}">
        <img ng-src="{{pinch.face}}">
        <h2>{{pinch.message}}</h2>
        <i class="icon ion-chevron-right icon-accessory"></i>

        <ion-option-button class="button-assertive" ng-click="remove(pinch)">
          Delete
        </ion-option-button>
      </ion-item>
    </ion-list>
  </ion-content>
</ion-view>
